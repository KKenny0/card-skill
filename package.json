{
  "name": "card-skill",
  "version": "0.2.11",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check-output": "node scripts/check-output.mjs --help",
    "package-skill": "node scripts/package-skill.mjs",
    "test": "node evals/check-assertions.mjs --self-test && node scripts/check-output.mjs --self-test && node scripts/validate.mjs",
    "validate": "node scripts/validate.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
